/// TCP port for your Skillmatch **Express** API (`PORT` in `backend/.env`).
/// Must match the port your backend actually listens on.
/// Override with `--dart-define=API_PORT=...` when needed.
const int kDefaultApiPort = 5002;
