/// TCP port for your Skillmatch **Express** API (`PORT` in `backend/.env`).
/// Must match the port printed when you run the web backend (e.g. "Server is running on port 5000").
/// Change this if your web backend uses a different port, or pass `--dart-define=API_PORT=3000`.
const int kDefaultApiPort = 5000;
